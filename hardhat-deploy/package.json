{
  "name": "reputation-dao-hardhat",
  "version": "1.0.0",
  "description": "Hardhat project for deploying Reputation DAO contracts",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network rootstock-testnet"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.8.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.5"
  }
}
